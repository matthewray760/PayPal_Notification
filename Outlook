import win32com.client


def omega_send_email(account_ids_non_matching):
    # Create an instance of the Outlook application
    outlook = win32com.client.Dispatch("Outlook.Application")

    account_ids = account_ids_non_matching
    
    # Create a new mail item
    mail = outlook.CreateItem(0)  # 0: olMailItem

    # Set the email parameters
    mail.To = "matthewray@clearwateranalytics.com"  # Your email address
    mail.Subject = "Omega TS Alert"
    mail.Body = f"Accounts: {' , '.join(map(str, account_ids_non_matching))} are not reconciled"

    #mail.Display()  # Opens the email for review
    mail.Importance = 2
   
    # Send the email
    mail.Send()  # This sends the


